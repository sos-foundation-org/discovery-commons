# Discovery Commons — UI/UX 與效能優化規劃

**日期：** 2026-09-16  
**參與專家：** Software Engineer / Product Designer / Devil's Advocate  
**狀態：** 效能修復已直接實施；UI/UX 設計變更待 Ping 確認

---

## 一、已直接修復的效能問題

以下項目已在本次修改中直接實施，`tsc --noEmit` 通過。Ping 在本機跑 `npm run build` 確認即可 push。

### 1.1 hero.png 壓縮（影響：首頁載入時間大幅改善）

| 檔案 | 大小 | 說明 |
|------|------|------|
| `hero.png`（原檔保留） | 2.27 MB | 1672×941 PNG，未壓縮 |
| `hero.webp`（新增） | 155 KB | 同尺寸 WebP，品質 82 |
| `hero-mobile.webp`（新增） | 56 KB | 836×471 手機版 |

**改動：** `globals.css` 中 `.dc-hero-image` 改用 WebP，手機 `@media (max-width: 640px)` 載入更小的版本。原 PNG 保留作為 fallback。

**【Software Engineer】** 這是整站最大的單一效能瓶頸。hero 圖從 2.27 MB 降到 155 KB（-93%），手機上降到 56 KB（-97%）。首頁 LCP（Largest Contentful Paint）預估改善 1-3 秒（取決於使用者網速）。

### 1.2 `next.config.mjs` 套件樹搖優化

新增 `experimental.optimizePackageImports`，確保 `lucide-react`、`date-fns`、Radix UI 元件庫等只打包實際使用的部分。

**預估改善：** Client bundle 減少 30-80 KB（gzipped），視原始 tree-shaking 效率而定。

### 1.3 Thread 詳情頁 — 重型元件 lazy loading

`ContributionForm`、`AIReviewSection`、`ReplicationSection` 改用 `next/dynamic` 延遲載入（`ssr: false`）。這些元件在頁面底部或需要登入才顯示，不需要首次渲染就載入。

**預估改善：** 初始 JS bundle 減少 ~40-60 KB，首屏互動時間（TTI）改善。

### 1.4 通知輪詢頻率調降

Navbar 的通知輪詢從 30 秒調為 60 秒。

**理由：** 對於一個學術協作平台，通知不需要即時性。60 秒已經足夠。減少一半的 API 請求，降低伺服器與 Supabase 的負擔。

### 1.5 Threads 頁面 — 消除冗餘查詢

原本 threads 頁面會先查詢**所有** threads 取得 domain tags，再查詢一次帶過濾條件的 threads。合併為單次查詢，domain tags 從已取得的結果中提取。

**注意：** domain tags 現在只反映當前可見的 threads（而非全站），這對使用者其實更直覺。如果需要全站 domain tags 可以另做快取。

---

## 二、效能優化建議清單（需設計決策或進一步工作）

按優先順序排列：

### P1 — 高影響

| # | 問題 | 專家 | 建議方案 | 預估改善 |
|---|------|------|----------|----------|
| 2.1 | `react-markdown` + `remark-gfm` 每個 contribution 都載入（~50 KB gzipped） | SE | 用 `next/dynamic` lazy-load `ContributionContent`；或短文本用簡易渲染器 | -50 KB client bundle |
| 2.2 | `@anthropic-ai/sdk` 在 dependencies 中（~200 KB） | SE | 確認只在 server-side route handlers 中使用。如果是，移到 `devDependencies` 或確保 Next.js 自動排除。如果不是，需要 dynamic import | 可能 -200 KB |
| 2.3 | Footer 的 SOS logo 用 `<img>` 而非 `next/image` | SE | 改用 `next/image`；SVG 可以直接 inline 更好 | 微小但消除 ESLint 警告 |
| 2.4 | AvatarBadge 的 `<img>` 沒用 `next/image` | SE | 改用 `next/image`，啟用自動 WebP 轉換和大小優化 | 有 OAuth 頭像時改善 |

### P2 — 中影響

| # | 問題 | 專家 | 建議方案 |
|---|------|------|----------|
| 2.5 | `date-fns` 在 dependencies 但 `timeAgo()` 和 `formatDate()` 是手寫的 | SE | 確認是否有其他地方用到 `date-fns`。若無，移除節省 ~15 KB |
| 2.6 | Body 背景的 `radial-gradient` 點陣圖案 | SE | 手機上可能造成重繪成本。建議在 `@media (max-width: 640px)` 中移除 |
| 2.7 | 沒有 Service Worker / PWA 支援 | SE | 未來可加入，讓重複訪問更快（prototype 階段非必要） |

### P3 — 低影響但值得注意

| # | 問題 | 專家 |
|---|------|------|
| 2.8 | 沒有 `<link rel="preconnect">` 到 Supabase | SE |
| 2.9 | 沒有 metadata 中的 `viewport` 主題色 | PD |

---

## 三、手機適配修復清單

### 已確認正常運作的部分
- Navbar 的 hamburger menu（md breakpoint）✓
- Footer 在 sm 以下正確堆疊 ✓
- 首頁 hero 文字 `text-4xl sm:text-6xl` 響應 ✓
- Thread 列表 ThreadRow 元件正常 ✓

### 需要修復的問題

| # | 頁面 | 問題 | 嚴重度 | 修復方案 | 可直接改？ |
|---|------|------|--------|----------|-----------|
| 3.1 | Thread 詳情頁 | Stage progress bar 在手機上 `flex-wrap` 後混亂（7 個 stage + 連接線）| 高 | 改為手機上水平捲動 `overflow-x-auto` 或改用簡化版（只顯示當前 stage） | ✅ 可直接改 |
| 3.2 | Thread 詳情頁 | 標題和 badges 的 `flex items-start justify-between` 在窄螢幕擠壓 | 高 | 改為 `flex-col` on mobile，badges 放下方 | ✅ |
| 3.3 | Contribution 卡片 | 作者行（avatar + badge + type + time + comments）太擁擠 | 中 | 調整 gap 和 wrap 策略 | ✅ |
| 3.4 | ContributionForm | 類型選擇按鈕在手機上需要兩行以上 | 中 | 已有 `flex-wrap` 但按鈕可以更小 `text-xs` on mobile | ✅ |
| 3.5 | About 頁 | 七階段卡片 grid `sm:grid-cols-2 lg:grid-cols-3` 在單欄時太長 | 低 | 可改為手機上 horizontal scroll carousel | 需設計 |
| 3.6 | About 頁 | Graduated Visibility 的 `sm:flex-row` 箭頭在手機上隱藏 | 低 | 加入垂直箭頭 | ✅ |
| 3.7 | New Thread 頁 | Domain tags 建議列表 16 個 badge 在手機上擁擠 | 低 | 加 `max-h` + `overflow-y-auto` 或預設收合 | ✅ |

**【Devil's Advocate】** Stage progress bar（3.1）是手機上最嚴重的問題。它在桌面上展示 research pipeline 很漂亮，但在手機上幾乎不可用。七個 stage 加六條連接線強制在 375px 寬度裡渲染，使用者看到的是一堆斷行的小方塊。建議手機上改為**只顯示當前 stage 的 pill + 完成百分比**，或改為水平可滾動。

---

## 四、UI/UX 設計改善建議

### 【Product Designer 核心判斷】
Discovery Commons 的資訊架構和互動流程已經相當成熟（seven-stage model、seal-reveal、graduated visibility）。主要問題在於**視覺層**：stock shadcn/ui 色彩、placeholder 內容、缺少品牌識別。這些不影響功能但嚴重影響第一印象和可信度。

### 4.1 品牌與色彩系統（高影響）

**現狀：** 使用 shadcn/ui 預設色彩（藍色系 primary），跟幾萬個其他 Next.js 專案看起來一模一樣。

**建議：**

| 元素 | 現狀 | 建議 |
|------|------|------|
| Primary | `hsl(221.2, 83.2%, 53.3%)` — 標準藍 | 改為 teal/深青色系（`hsl(174, 72%, 40%)`），呼應自然/科學主題 |
| Accent | 與 secondary 相同 | 加入 warm amber（`hsl(38, 92%, 50%)`）作為 accent，與 teal 互補 |
| 背景點陣圖案 | 灰色圓點 | 改為更淡的、有機感的紋理（或直接移除） |
| Dark mode | 有 CSS 但無切換方式 | 加入 theme toggle 按鈕（放在 Navbar） |

**【Devil's Advocate】** 但要小心不要過度設計色彩。學術平台的使用者重視的是**可讀性和可信度**，不是「好看」。色彩改版必須：(1) 保持高對比度（WCAG AA）；(2) stage 色彩（blue/green/purple/cyan/amber/yellow/orange）是核心功能指示，不能被品牌色壓過。

### 4.2 Placeholder 內容替換（高影響）

這是可信度最大的傷口。

| 位置 | 現狀 | 建議 |
|------|------|------|
| 首頁 Antilibrary 區塊 | `"?"` + 文字描述 placeholder | 需要一張插圖：「巨大圖書館中，最有價值的書架標著 Unknown」 |
| About 頁三張 placeholder | 漸層色塊 + 文字描述 | 需要三張插圖（見下方圖片需求清單） |
| 首頁 Testimonials | "Dr. A. Researcher"、"Dr. B. Scientist" 假名 | 選項：(1) 移除整個區塊直到有真實 testimonials；(2) 改為「Early Adopter Feedback」框架，預留填空位 |

**【Devil's Advocate】** Placeholder testimonials 比沒有 testimonials 更糟。帶假名的假引言會被任何認真的研究者視為紅旗。**強烈建議移除**，改為 「Join X researchers exploring Y threads」的即時統計。

### 4.3 Logo 與 Favicon（中高影響）

**現狀：** Logo 是一個藍色方塊裡的 "DC" 文字。無 favicon。

**建議：**
- 設計一個簡約 logo mark：建議概念是「互相連結的問號」或「望遠鏡/放大鏡看向未知」
- Favicon 使用 logo mark 的簡化版
- 可以用 SVG 自動生成（見圖片需求清單）

### 4.4 排版與間距微調（中影響）

| 元素 | 問題 | 建議 |
|------|------|------|
| 首頁 hero | `py-24` 略矮，英雄感不足 | 改為 `py-32` 或 `min-h-[60vh]` |
| 首頁 stats | 三個數字缺乏視覺重量 | 加入 icon 或微動畫（count-up effect） |
| Thread 卡片 | 資訊密度適中但缺乏視覺層次 | 給 stage badge 加入對應的左側色條 — 已有（`ThreadRow` 有 discipline dot）✓ |
| Contribution 卡片 | hash 行的 `font-mono text-xs` 太小不易讀 | 可保持但加入 copy button hover 效果 |
| Covenant 區塊 | 6 個 principle 視覺太單調 | 每個前面加對應的 icon（checkmark 已有，但太統一） |

### 4.5 互動回饋改善（中影響）

| 元素 | 問題 | 建議 |
|------|------|------|
| Like 按鈕 | 有 optimistic update 但無動畫 | 加入 scale bounce 微動畫 |
| 送出 contribution | "Submitting..." 文字 | 加入 loading spinner |
| Seal 操作 | 缺少視覺確認 | 加入 lock animation 或 confetti hint |
| Page transitions | 有 loading.tsx skeleton 但轉場生硬 | 考慮加入 NProgress-style top bar |

### 4.6 資訊架構建議（低影響，未來方向）

**【Product Designer】**
- About 頁太長（370 行）：建議拆分為多個 tab 或 accordion sections
- 新使用者 onboarding 缺失：第一次登入應該有 guided tour 或 wizard
- Thread 列表缺乏「最近活躍」vs「最新建立」的排序切換
- 搜尋功能是 server-side（每次搜尋觸發完整頁面重載）：考慮 debounced client-side filter

---

## 五、圖片 / 圖示需求清單

### 可以 SVG 自動生成（Ping 不需額外提供）

| # | 用途 | 描述 | 格式 |
|---|------|------|------|
| I-1 | Logo mark | 簡約幾何圖形：互連的節點 / 問號變體 / 望遠鏡 | SVG |
| I-2 | Favicon | Logo mark 簡化版，16x16 / 32x32 / 192x192 | SVG → ICO/PNG |
| I-3 | Stage icons（7 個） | Question（?）、Hypothesis（💡）、Data（📊）、Simulation（🖥）、Statistics（📈）、Interpretation（🔍）、Insight（✨）的簡約線條 icon | SVG |
| I-4 | Covenant icons（6-9 個） | 對應每條 covenant principle 的小 icon | SVG |
| I-5 | Empty state illustrations | 「無 threads」「無 contributions」「無 notifications」的輕量插圖 | SVG |

### 需要 Ping 提供或用 AI 生成

| # | 用途 | 描述 | 建議尺寸 | AI Prompt 建議 |
|---|------|------|----------|----------------|
| I-6 | 首頁 Antilibrary 插圖 | 一座巨大圖書館，最有價值的書架標著 "Unknown" | 600×400 | 見下方 prompt |
| I-7 | About 頁 hero | 一個成長中的互連想法網路 | 1200×400 | 見下方 prompt |
| I-8 | About 頁 pipeline 圖 | Q → H → D/S → St → I → In 的流程圖（不是 placeholder 文字） | 1200×300 | 可用 SVG 自動生成 |
| I-9 | About 頁 community 照片 | 「田野自然觀察者、理論學家、好奇公民一起構建想法」 | 1200×400 | 見下方 prompt |
| I-10 | OG Image（社群分享預覽） | Discovery Commons 品牌圖 + tagline | 1200×630 | 見下方 prompt |

### AI 圖片生成 Prompt 包

| # | 用途 | 目標模型 | 正向 Prompt | 負向 Prompt | Aspect |
|---|------|----------|-------------|-------------|--------|
| I-6 | Antilibrary 插圖 | DALL·E 3 / MJ v7 | `Vast, luminous library with towering bookshelves disappearing into mist. One prominent shelf in the center glows softly, labeled "Unknown" in elegant serif. Warm amber and cool teal lighting, volumetric fog, cinematic wide angle, digital painting style, muted earth tones with occasional bright accents` | `text other than Unknown, people, photorealistic, oversaturated, cartoon` | 3:2 |
| I-9 | Community 插圖 | DALL·E 3 / MJ v7 | `Three diverse researchers collaborating around a holographic display of interconnected nodes: a field naturalist with binoculars and journal, a theorist with equations, and a curious citizen with a laptop. Warm study room setting, bookshelves in background, soft directional light, editorial illustration style, teal and amber color palette` | `cartoon, anime, photorealistic face details, brand logos, text` | 3:1 |
| I-10 | OG Image | DALL·E 3 | `Abstract scientific network visualization with interconnected nodes forming a question mark shape, deep teal gradient background (#0d9488) fading to dark, subtle constellation-like dots, clean minimalist design suitable for social media preview card, wide format` | `text, logos, faces, photorealistic, busy patterns` | 1200:630 |

---

## 六、Devil's Advocate — 整體風險評估

### 「如果我是你最強的反對者」

1. **最大風險不是技術，是空殼感。** 目前網站的 placeholder 內容（假 testimonials、漸層佔位符、"DC" 方塊 logo）加總起來傳達的訊息是「這是一個還在施工的 side project」。學術使用者的信任門檻很高——他們會在 10 秒內判斷這是否值得投入智慧財產。

2. **效能修復不會改變 conversion。** Hero 圖從 2.2 MB 變成 155 KB 會讓首頁快 2 秒，但如果使用者看到的是假人名 testimonials 和 placeholder 圖片，他們不會因為頁面變快就註冊。效能是 hygiene factor（門檻因素），不是 motivator。

3. **手機適配問題被低估了。** 學術使用者可能在通勤、會議間、田野中用手機看到別人分享的 thread 連結。如果 stage progress bar 在手機上是一團亂碼，他們對平台嚴謹度的印象就定型了。

4. **Dark mode 缺失是 developer 信號。** 2026 年沒有 dark mode toggle 的網站，在技術社群裡會被視為「不專業」。CSS 已經準備好了，只差一個按鈕。

5. **逆轉觸發條件：** 如果投入 UI 美化但 6 個月後仍然沒有真實使用者和真實 testimonials，問題不在 UI 而在 product-market fit。建議平行進行：UI 美化 + 找 5 個 beta tester 寫真實 testimonials。

---

## 七、執行優先序與時間估計

### 第一波（可直接執行，1-2 天）— 已完成 ✅

- [x] hero.png WebP 壓縮（-93% 大小）
- [x] next.config 套件優化
- [x] 重型元件 lazy loading
- [x] 通知輪詢調降
- [x] Threads 頁冗餘查詢消除

### 第二波（純程式碼，無需設計決策，2-3 天）

- [ ] 手機 stage progress bar 修復（3.1）
- [ ] Thread 詳情頁手機 header 響應式修復（3.2, 3.3）
- [ ] Dark mode toggle 按鈕
- [ ] 移除 placeholder testimonials，改為即時統計
- [ ] `react-markdown` dynamic import（2.1）
- [ ] AvatarBadge 改用 `next/image`（2.4）
- [ ] Footer SOS logo inline SVG 或 `next/image`（2.3）

### 第三波（需要設計決策 / 圖片資源，3-5 天）

- [ ] 品牌色彩系統更新（4.1）— 需 Ping 確認 teal/amber 方向
- [ ] Logo 與 favicon 設計（4.3）— 可先用 SVG 自動生成 prototype
- [ ] Placeholder 插圖替換（4.2, I-6 ~ I-10）— 需 Ping 用 AI 生成或提供
- [ ] Stage icons 設計（I-3）
- [ ] Like / Seal 微動畫（4.5）

### 第四波（中長期改善）

- [ ] About 頁重構（4.6）
- [ ] 新使用者 onboarding flow
- [ ] Thread 列表排序切換
- [ ] OG Image / 社群分享預覽優化

---

## 附錄：修改的檔案清單

| 檔案 | 變更摘要 |
|------|----------|
| `public/images/hero.webp` | 新增 — hero 圖片 WebP 版本（155 KB） |
| `public/images/hero-mobile.webp` | 新增 — 手機版 hero（56 KB） |
| `public/images/hero_optimized.png` | 新增 — 優化後的 PNG（可刪除，WebP 更好） |
| `src/app/globals.css` | `.dc-hero-image` 改用 WebP + 手機媒體查詢 |
| `next.config.mjs` | 新增 `experimental.optimizePackageImports` |
| `src/components/navbar.tsx` | 通知輪詢 30s → 60s |
| `src/app/threads/[threadId]/page.tsx` | 三個重型元件改 `next/dynamic` lazy load |
| `src/app/threads/page.tsx` | 消除冗餘的全站 thread 查詢 |
